import styled from 'styled-components'

export const ContentThirdTitle = styled.h3`

  font-size: 2.2rem;
  font-weight: 400;
  color: var(--third-color);

  @media (max-width: 480px) {
    font-size: 2rem;
  }
`